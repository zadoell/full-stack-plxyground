{
  "name": "plxyground-admin",
  "version": "1.0.0",
  "description": "PLXYGROUND Admin Panel",
  "scripts": {
    "start": "npx serve -s . -l 3012 --no-clipboard",
    "build": "mkdir -p dist && cp -r index.html css js dist/"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
